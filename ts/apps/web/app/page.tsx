import { TradePageContent } from "@/components/TradePageContent";

export default function Home() {
  return (
    <TradePageContent
      title="Bunker Cash"
      description="Bunker Cash provides eligible participants with a controlled interface to buy protocol tokens and submit sell requests under the protocol's access, settlement, and liquidity rules."
    />
  );
}
